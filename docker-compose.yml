services:
  baileys:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - whatsapp_auth:/data/whatsapp-auth
    mem_limit: 400m
    memswap_limit: 400m
    init: true
    stop_grace_period: 30s

volumes:
  whatsapp_auth:
